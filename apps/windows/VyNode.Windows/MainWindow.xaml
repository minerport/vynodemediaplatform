<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="VyNode.Windows.MainWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="VyNode">
  <Grid x:Name="Root" Background="{StaticResource VyBackground}">
    <Grid.RowDefinitions><RowDefinition x:Name="TitleRow" Height="48"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <Grid x:Name="TitleRegion" Background="{StaticResource VyBackground}">
      <TextBlock Text="VyNode" Foreground="{StaticResource VyText}" FontWeight="SemiBold" FontSize="15" Margin="18,0" VerticalAlignment="Center" />
    </Grid>
    <Frame x:Name="ContentFrame" Grid.Row="1" />
  </Grid>
</Window>

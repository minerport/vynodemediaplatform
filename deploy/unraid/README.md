# Unraid foundation

The XML consumes the same OCI image as every Docker installation. Install it as a
private template until an image and Community Applications entry are published.
GPU mapping may eventually use device/runtime settings, but Phase 0 performs no
hardware discovery or transcoding.

